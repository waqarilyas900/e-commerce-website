# Site brand (logo + name) — used in header, footer, checkout, SEO, and JSON-LD.
# Copy to `.env.local` and restart `npm run dev` after changes.
NEXT_PUBLIC_SITE_NAME=MELALOOTLO
NEXT_PUBLIC_LOGO_URL=/melalootlo_logo.jpeg
