"use client";

/**
 * Legacy module path — same as `AddToCartVariantButton`.
 * Prefer importing from `AddToCartVariantButton` or `AddToCartButton` directly.
 */
import { AddToCartVariantButton } from "./AddToCartVariantButton";

export const ProductPurchaseControls = AddToCartVariantButton;
export default AddToCartVariantButton;
